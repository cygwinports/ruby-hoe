ORIG_PN=hoe
inherit ruby

DESCRIPTION="Rake/RubyGems helper"
HOMEPAGE="http://seattlerb.rubyforge.org/hoe/"
SRC_URI="mirror://rubyforge/seattlerb/${ORIG_PN}-${PV}.tgz"

src_install() {
	cd ${B}
	dobin bin/sow

	dodir ${RUBY_SITELIB}
	cp -rL lib/* ${D}${RUBY_SITELIB}
}
